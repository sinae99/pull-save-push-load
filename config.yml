# Push-load Configuration File
# Customize this file for your deployment

# ---------------------------------------------------------------------------
# TARGET HOSTS
# ---------------------------------------------------------------------------
target_hosts:
  - name: vm1
    ip: 37.32.14.28

# ---------------------------------------------------------------------------
# OPERATION MODE: "tarballs", "images", or "mixed"
# ---------------------------------------------------------------------------
mode: "images"

# ---------------------------------------------------------------------------
# TARBALL CONFIGURATION (for mode: "tarballs" or "mixed")
# ---------------------------------------------------------------------------
# Uncomment and set if using existing tarballs:
# tarballs_dir: "/path/to/docker-images"

# ---------------------------------------------------------------------------
# IMAGE CONFIGURATION (for mode: "images" or "mixed")
# ---------------------------------------------------------------------------
docker_images:
  - busybox:latest

skip_pull: false
tarball_dir: "/tmp/docker-push-load"

# ---------------------------------------------------------------------------
# REMOTE CONFIGURATION
# ---------------------------------------------------------------------------
remote_user: "ubuntu"
